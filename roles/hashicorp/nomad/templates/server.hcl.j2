server {
  enabled          = true
  bootstrap_expect = {{ groups['nomad_servers'] | length }}
  encrypt          = "{{ gossip_keyring.stdout }}"
}

client {
  enabled = false
}

acl {
  enabled = true
}
